# Auth with polkit-gnome:
# exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Cool sway stuff
# exec env RUST_BACKTRACE=1 RUST_LOG=swayr=debug swayrd > /tmp/swayrd.log 2>&1

exec ulauncher --hide-window

# Workstyle
exec_always --no-startup-id workstyle &> /tmp/workstyle.log 2>&1

# Wlsunset
exec ~/.config/sway/scripts/light.sh

# Clipman
# exec clipman restore
# exec wl-paste -t text --watch clipman store

# Gnome Keyring
exec gnome-keyring-daemon --daemonize --start
# exec eval $(gnome-keyring-daemon --start)
# exec export SSH_AUTH_SOCK

# Outputs
exec_always 'killall kanshi; kanshi'

# Schatchterm
exec kitty -o background_opacity=0.95 -o remember_window_size=no -o initial_window_width=120c -o initial_window_height=40c -T scratchterm

exec todoist

# Auto mount usb
exec udiskie

###################
#  Theme related  #
###################

# Desktop notifications
# exec_always ~/.config/sway/scripts/mako.sh
# WOB
exec_always ~/.config/sway/theme/wob.sh
